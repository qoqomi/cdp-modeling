# OpenAI
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Embedding / Rerank service (external)
# - Used by backend/model/rag/embedding_client.py
# - Must expose: GET /health, POST /embed, POST /rerank
BGE_EMBEDDING_URL=http://localhost:5002

# Optional: Spring Boot API (questionnaire sync target)
SPRING_API_URL=http://localhost:8080
